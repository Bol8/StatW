@model IEnumerable<Repository.Liga>

@*<p>
    @Html.ActionLink("Actualizar", "Update","AppManage")
</p>*@
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Nombre)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Pais)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Temporada1.Nombre)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @*@Html.DisplayFor(modelItem => item.Nombre)*@
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Pais)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Temporada1.Nombre)
        </td>
        <td>
            @Html.ActionLink("Teams", "Index","Equipo", new { league = item.IdLiga },null) |
            @Html.ActionLink("Prognostics","Index", "Statistics", new { league = item.IdLiga },null) 
            @*@Html.ActionLink("Delete", "Delete", new { id=item.IdLiga })*@
        </td>
    </tr>
}

</table>
